<UserControl x:Class="CodexBridge.App.Controls.DraftPanel" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource PanelBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,0,0" Padding="12">
        <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <DockPanel><TextBlock Text="Draft Preview" FontFamily="Segoe UI Semibold" FontSize="16" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right"><Button Content="Copy" Command="{Binding CopyDraftCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="12,5" Margin="0,0,8,0" /><Button x:Name="ToggleButton" Content="Collapse" Click="ToggleClick" Style="{StaticResource SecondaryButtonStyle}" Padding="10,5" /></StackPanel>
            </DockPanel>
            <Grid x:Name="DraftBody" Grid.Row="1" Margin="0,12,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <TextBlock Text="{Binding SelectedTurnSummary}" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,8" />
                <Grid Grid.Row="1"><TextBox Text="{Binding DraftText, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" BorderBrush="{DynamicResource BorderBrush}" Background="{DynamicResource Background}" Foreground="{DynamicResource TextPrimary}" Padding="12" FontFamily="Consolas" FontSize="13" />
                    <TextBlock Text="No draft selected" Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding DraftText}" Value=""><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
                </Grid>
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0"><TextBlock Text="{Binding DraftText.Length, StringFormat=Characters: {0}}" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,12,0" /><Button Content="Apply Patch" IsEnabled="False" Style="{StaticResource DisabledButtonStyle}" Padding="12,5" Margin="0,0,8,0" /><Button Content="Send To Codex" IsEnabled="False" Style="{StaticResource DisabledButtonStyle}" Padding="12,5" /></StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
