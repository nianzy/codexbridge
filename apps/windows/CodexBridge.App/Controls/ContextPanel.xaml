<UserControl x:Class="CodexBridge.App.Controls.ContextPanel" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Padding="12" Background="{StaticResource PanelBackground}"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DockPanel><TextBlock Text="Context" FontFamily="Segoe UI Semibold" FontSize="16"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right"><Button Content="Preview" Command="{Binding PreviewContextCommand}" Padding="8,4" Margin="0,0,6,0"/><Button Content="Copy Context" Command="{Binding CopyContextCommand}" Padding="8,4" Margin="0,0,6,0"/><Button Content="Export Context" Command="{Binding ExportContextCommand}" Padding="8,4"/></StackPanel></DockPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8"><TextBlock Text="{Binding ContextItemCount, StringFormat={}Selected: {0} items}"/><TextBlock Text="{Binding ContextCharacters, StringFormat={}{0} chars}" Margin="12,0,0,0"/><TextBlock Text="{Binding ContextSizeState}" Margin="12,0,0,0"/></StackPanel>
        <Grid Grid.Row="2"><ListBox ItemsSource="{Binding ContextItems}" BorderThickness="0"><ListBox.ItemTemplate><DataTemplate><DockPanel Margin="2"><Button Content="×" Tag="{Binding}" Click="RemoveClick" DockPanel.Dock="Right" Padding="4,0"/><TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis"/></DockPanel></DataTemplate></ListBox.ItemTemplate></ListBox><TextBlock Text="No context selected&#x0a;Select session turns, files, diffs, or notes to build a context pack." TextAlignment="Center" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding ContextItemCount}" Value="0"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></Grid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="Clear" Command="{Binding ClearContextCommand}" Padding="10,4"/></StackPanel>
    </Grid></Border>
</UserControl>
