<UserControl x:Class="CodexBridge.App.Controls.CodeBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#101828" CornerRadius="8" Padding="12" Margin="0,8">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <DockPanel>
                <TextBlock Text="{Binding Language, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="#98A2B3" FontSize="12" />
                <Button x:Name="CopyButton" Content="Copy" Click="CopyClick" HorizontalAlignment="Right" DockPanel.Dock="Right" Padding="10,3" />
            </DockPanel>
            <TextBox Grid.Row="1" Text="{Binding Code, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}" IsReadOnly="True"
                     BorderThickness="0" Background="Transparent" Foreground="#F2F4F7" FontFamily="Consolas" FontSize="13"
                     TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Padding="0,8,0,0" />
        </Grid>
    </Border>
</UserControl>
